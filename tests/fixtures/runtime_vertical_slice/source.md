# Runtime Adapter Fixture Source

This offline source documents that the Phase 3 runtime can gather auditable
local evidence without network access.

The fixture metric is reproducibility: every adapter output must be traced to a
snapshot and a task contract.
